import CategoryComponent from "./CategoryComponent";

import React from "react";

export default {
  title: "Components/CategoryComponent",
  component: CategoryComponent,
};

export const Primary = {
  render: () => <CategoryComponent />,
};
